"""
Main entry point for the activation_extraction module.

This allows running the module directly with `python -m activation_extraction`.
"""

from .run_batch_and_verify import main

if __name__ == "__main__":
    main() 